import { createTheme } from '@mui/material/styles';


export const theme = createTheme({
    palette: {
        primary: {
            main: '#0CC9E8', // Your hex color code
        },
        // You can add other color definitions if needed
    },
});